{% extends 'base.html.twig' %}

{% block title %}Importer des produits{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto py-12 px-4">
    <div class="bg-white shadow rounded-xl p-8 border border-gray-100">
        <h1 class="text-2xl font-bold mb-4">Importer des produits (CSV)</h1>
        <form method="post" enctype="multipart/form-data">
            <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700 mb-2">Fichier CSV</label>
                <input type="file" name="csv_file" accept=".csv,text/csv" class="block w-full text-sm text-gray-500 file:bg-white file:border file:px-3 file:py-2 file:rounded file:border-gray-300" />
            </div>
            <div class="flex gap-3">
                <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded">Importer</button>
                <a href="{{ path('app_product_index') }}" class="text-gray-600 px-4 py-2 rounded border border-gray-200">Annuler</a>
            </div>
        </form>

        <div class="mt-6 text-sm text-gray-500">
            Le fichier doit être délimité par des points-virgules (;) et contenir l'en-tête : id,name,description,price,type,weight,height,width,downloadUrl,licenseKey
        </div>
    </div>
</div>
{% endblock %}

