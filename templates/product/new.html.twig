{% extends 'base.html.twig' %}

{% block title %}Nouveau produit{% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto py-12 px-4">
    <div class="mb-6">
        <a href="{{ path('app_product_index') }}" class="text-sm font-medium text-indigo-600 hover:text-indigo-800 transition flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            Retour au catalogue
        </a>
    </div>

    <div class="bg-white shadow-2xl rounded-3xl overflow-hidden border border-gray-100">
        <div class="bg-indigo-600 px-8 py-6">
            <h1 class="text-2xl font-bold text-white">Créer un nouveau produit</h1>
            <p class="text-indigo-100 text-sm opacity-80">Ajoutez les informations du produit</p>
        </div>

        <div class="p-8">
            {{ include('product/_form.html.twig', {'button_label': 'Créer le produit'}) }}
        </div>
    </div>
</div>
{% endblock %}
